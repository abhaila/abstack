{
  "name": "sequence-database",
  "description": "Query AlloyDB read replicas for debugging and SRE purposes. Supports dev, sandbox, and production environments.",
  "version": "1.1.0",
  "mcpServers": {
    "sequence-database": {
      "command": "npx",
      "args": [
        "tsx",
        "${CLAUDE_PLUGIN_ROOT}/src/index.ts"
      ]
    }
  }
}
